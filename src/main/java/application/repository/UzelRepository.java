package application.repository;

import application.entity.goods.Uzel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UzelRepository extends JpaRepository<Uzel,Integer> {
}
